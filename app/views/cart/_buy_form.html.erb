<%= form_with url: buy_path(product), method: :post do |f| %>
  <%= f.hidden_field :update_action, value: 'buy' %>
  <%= f.number_field :amount, class:'input-field w-[100px] h-[37px] inline-block' %>
  <%= f.submit "Add to cart", class:"black-button" %>
<% end %>
