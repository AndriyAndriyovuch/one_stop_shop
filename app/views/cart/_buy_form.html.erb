<%= form_with url: add_product_in_cart_path(product), method: :post do |f| %>
  <%= f.number_field :amount, class:'input-field w-24 h-9 inline-block' %>
  <%= f.submit "Add to cart", class:"btn-black" %>
<% end %>
